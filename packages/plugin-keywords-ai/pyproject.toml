[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neocortex-keywordsai"
version = "0.1.0"
description = "Keywords AI / OpenAI JSON Schema integrations for TinyHumans memory API"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
dependencies = [
    "tinyhumansai>=0.5",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio"]
examples = ["keywordsai", "python-dotenv>=1.0"]

[tool.setuptools.packages.find]
include = ["neocortex_keywordsai*"]

[tool.ruff]
line-length = 88
target-version = "py39"
