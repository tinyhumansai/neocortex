identity:
  name: chat_memory_context
  author: Neocortex
  label:
    en_US: Chat Memory Context
description:
  human:
    en_US: Chat with Neocortex contextual memory.
  llm:
    en_US: Chat with memory context via POST /v1/memory/conversations.
parameters:
  - name: messages_json
    type: string
    required: true
    label:
      en_US: Messages (JSON)
  - name: temperature
    type: number
    required: false
    label:
      en_US: Temperature
  - name: max_tokens
    type: number
    required: false
    label:
      en_US: Max Tokens

