identity:
  name: chat_memory
  author: Neocortex
  label:
    en_US: Chat Memory
description:
  human:
    en_US: Chat with memory cache.
  llm:
    en_US: Chat with memory cache via POST /v1/memory/chat.
parameters:
  - name: messages_json
    type: string
    required: true
    label:
      en_US: Messages (JSON)
  - name: temperature
    type: number
    required: false
    label:
      en_US: Temperature
  - name: max_tokens
    type: number
    required: false
    label:
      en_US: Max Tokens

