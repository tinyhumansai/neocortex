identity:
  name: get_ingestion_job
  author: Neocortex
  label:
    en_US: Get Ingestion Job
description:
  human:
    en_US: Get ingestion job status by job_id.
  llm:
    en_US: Get ingestion job via GET /v1/memory/ingestion/jobs/:jobId.
parameters:
  - name: job_id
    type: string
    required: true
    label:
      en_US: Job ID

