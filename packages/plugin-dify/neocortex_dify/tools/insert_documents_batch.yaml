identity:
  name: insert_documents_batch
  author: Neocortex
  label:
    en_US: Insert Documents Batch
description:
  human:
    en_US: Insert multiple documents in one call. Each item must include document_id.
  llm:
    en_US: Insert multiple documents via /v1/memory/documents/batch. Each item requires document_id.
parameters:
  - name: items_json
    type: string
    required: true
    label:
      en_US: Items (JSON)
    human_description:
      en_US: JSON array of document items.
    llm_description:
      en_US: JSON array of document items; each item must include document_id.

