{
  "name": "@tinyhumansai/neocortex",
  "version": "0.1.4",
  "description": "TypeScript SDK for TinyHumans Neocortex API - insert, query, delete, recall",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build",
    "test:routes": "npm run build && node scripts/test-routes.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
